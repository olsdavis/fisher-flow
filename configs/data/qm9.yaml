_target_: src.data.DNAEnhancerDataModule
dataset: "FlyBrain" # choices are "MEL2" and "FlyBrain"
data_dir: ${paths.data_dir}/the_code/General/data/Deep
batch_size: 256 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 4
pin_memory: false
subset_train_as_val: true

