_target_: src.data.PromoterDesignDataModule
data_dir: ${paths.data_dir}/promoter_design
batch_size: 256 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 4
pin_memory: True
